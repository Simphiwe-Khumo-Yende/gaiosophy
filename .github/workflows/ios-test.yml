name: iOS Build Test (Step by Step)

on:
  workflow_dispatch: # Manual trigger only for testing
    inputs:
      test_phase:
        description: 'Which phase to test'
        required: true
        default: 'all'
        type: choice
        options:
        - 'dependencies'
        - 'build'
        - 'archive'
        - 'all'

env:
  FLUTTER_VERSION: '3.32.0'

jobs:
  test-ios-build:
    runs-on: macos-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: ${{ env.FLUTTER_VERSION }}
        channel: 'stable'
        cache: true

    - name: 🔍 Phase 1 - Validate Project Structure
      run: |
        echo "=== VALIDATING PROJECT STRUCTURE ==="
        echo "✅ Checking critical iOS files..."
        
        # Check essential files exist
        [ -f "ios/Runner.xcworkspace/contents.xcworkspacedata" ] && echo "✅ Xcode workspace exists" || echo "❌ Xcode workspace MISSING"
        [ -f "ios/Runner.xcodeproj/project.pbxproj" ] && echo "✅ Xcode project exists" || echo "❌ Xcode project MISSING"
        [ -f "ios/Runner/Info.plist" ] && echo "✅ Info.plist exists" || echo "❌ Info.plist MISSING"
        [ -f "ios/ExportOptions.plist" ] && echo "✅ ExportOptions.plist exists" || echo "❌ ExportOptions.plist MISSING"
        
        # Check DEVELOPMENT_TEAM in project
        if grep -q "DEVELOPMENT_TEAM = Z9WJ3V7JV3" ios/Runner.xcodeproj/project.pbxproj; then
          TEAM_COUNT=$(grep -c "DEVELOPMENT_TEAM = Z9WJ3V7JV3" ios/Runner.xcodeproj/project.pbxproj)
          echo "✅ DEVELOPMENT_TEAM configured ($TEAM_COUNT instances)"
        else
          echo "❌ DEVELOPMENT_TEAM MISSING"
          exit 1
        fi
        
        echo "✅ Phase 1 Complete: Project structure validated"

    - name: 🔍 Phase 2 - Test Dependencies
      if: github.event.inputs.test_phase == 'dependencies' || github.event.inputs.test_phase == 'all'
      run: |
        echo "=== TESTING DEPENDENCIES ==="
        
        # Test Flutter dependencies
        echo "📦 Running flutter pub get..."
        flutter pub get
        
        # Test CocoaPods
        echo "🍫 Testing CocoaPods installation..."
        cd ios
        rm -rf Pods/
        rm -f Podfile.lock
        pod install --verbose
        cd ..
        
        echo "✅ Phase 2 Complete: Dependencies resolved"

    - name: 🔍 Phase 3 - Test Flutter Build
      if: github.event.inputs.test_phase == 'build' || github.event.inputs.test_phase == 'all'
      run: |
        echo "=== TESTING FLUTTER BUILD ==="
        
        # Clean build
        flutter clean
        flutter pub get
        
        # Test Flutter iOS build (no codesigning)
        echo "🏗️ Building iOS app without codesigning..."
        flutter build ios --release --no-codesign --verbose
        
        echo "✅ Phase 3 Complete: Flutter build successful"

    - name: 🔍 Phase 4 - Test Code Signing Setup
      if: github.event.inputs.test_phase == 'archive' || github.event.inputs.test_phase == 'all'
      run: |
        echo "=== TESTING CODE SIGNING SETUP ==="
        
        # Check if secrets are available (without exposing them)
        if [ -z "${{ secrets.APPSTORE_ISSUER_ID }}" ]; then
          echo "❌ APPSTORE_ISSUER_ID secret missing"
          exit 1
        else
          echo "✅ APPSTORE_ISSUER_ID secret configured"
        fi
        
        if [ -z "${{ secrets.APPSTORE_KEY_ID }}" ]; then
          echo "❌ APPSTORE_KEY_ID secret missing"
          exit 1
        else
          echo "✅ APPSTORE_KEY_ID secret configured"
        fi
        
        if [ -z "${{ secrets.APPSTORE_PRIVATE_KEY }}" ]; then
          echo "❌ APPSTORE_PRIVATE_KEY secret missing"
          exit 1
        else
          echo "✅ APPSTORE_PRIVATE_KEY secret configured"
        fi
        
        # Setup keychain
        security create-keychain -p "temp123" build.keychain
        security default-keychain -s build.keychain
        security unlock-keychain -p "temp123" build.keychain
        security set-keychain-settings -t 3600 -u build.keychain
        
        # Create API key file
        mkdir -p ~/.appstoreconnect/private_keys/
        echo '${{ secrets.APPSTORE_PRIVATE_KEY }}' > ~/.appstoreconnect/private_keys/AuthKey_${{ secrets.APPSTORE_KEY_ID }}.p8
        chmod 600 ~/.appstoreconnect/private_keys/AuthKey_${{ secrets.APPSTORE_KEY_ID }}.p8
        
        # Test API connection
        echo "🔐 Testing App Store Connect API..."
        xcrun altool --list-apps \
          --apiKey ${{ secrets.APPSTORE_KEY_ID }} \
          --apiIssuer ${{ secrets.APPSTORE_ISSUER_ID }} \
          --verbose
        
        echo "✅ Phase 4 Complete: API connection successful"

    - name: 🔍 Phase 5 - Test Archive Creation
      if: github.event.inputs.test_phase == 'archive' || github.event.inputs.test_phase == 'all'
      run: |
        echo "=== TESTING ARCHIVE CREATION ==="
        
        # Test xcodebuild archive
        echo "📦 Creating Xcode archive..."
        xcodebuild -workspace ios/Runner.xcworkspace \
          -scheme Runner \
          -configuration Release \
          -destination generic/platform=iOS \
          -archivePath build/Runner.xcarchive \
          -allowProvisioningUpdates \
          CODE_SIGN_STYLE=Automatic \
          DEVELOPMENT_TEAM=Z9WJ3V7JV3 \
          archive
        
        # Check if archive was created
        if [ -d "build/Runner.xcarchive" ]; then
          echo "✅ Archive created successfully"
          ls -la build/Runner.xcarchive
        else
          echo "❌ Archive creation failed"
          exit 1
        fi
        
        echo "✅ Phase 5 Complete: Archive creation successful"

    - name: 🎯 Test Summary
      if: always()
      run: |
        echo "=== TEST SUMMARY ==="
        echo "Test phase: ${{ github.event.inputs.test_phase }}"
        echo "Runner: macOS-latest"
        echo "Flutter: ${{ env.FLUTTER_VERSION }}"
        echo "Xcode: $(xcodebuild -version | head -1)"
        echo "============================="
        
        if [ "$?" -eq 0 ]; then
          echo "🎉 ALL TESTS PASSED!"
          echo "Ready for full deployment workflow"
        else
          echo "❌ Tests failed - check logs above"
        fi

    - name: Upload build artifacts (if created)
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: ios-test-artifacts
        path: |
          build/Runner.xcarchive
          build/ios/iphoneos/Runner.app
        if-no-files-found: ignore

    - name: Clean up
      if: always()
      run: |
        rm -f ~/.appstoreconnect/private_keys/AuthKey_${{ secrets.APPSTORE_KEY_ID }}.p8
        security delete-keychain build.keychain || true
